import type { Config } from 'tailwindcss'

export default {
  content: [
    './index.html',
    './src/**/*.{vue,js,ts,jsx,tsx}',
  ],
  // No theme configuration needed - handled in CSS with @theme directive
} satisfies Config